#include <vector>

using namespace std;

vector<int> DP;

int solve(int num) {
    if (num==1) return DP[num]=num;
    if(DP[num]==0) {
        DP[num]=solve(num/2);
        if(num%2==1) DP[num]++;
    }

    return DP[num];
}

int solution(int n)
{
    DP.resize(n+1,0);
    return solve(n);
}
